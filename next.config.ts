import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  output: 'standalone',
};

module.exports = {
  output: 'export',
  trailingSlash: true,
};


export default nextConfig;
